import React from 'react'

const Header = () => {
  const heederStyle = {
    backgroundColor: '#333',
    color: '#fff'
  }
  return (
    <header style={heederStyle}>
        <h1>Groceries List</h1>
    </header>
  )
}

export default Header